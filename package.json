{
  "name": "doctorguidance-profile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "render:profile": "node scripts/render-profile.mjs",
    "check:canvas": "node --check ascii-editor/vignette-bloom.mjs && node --check scripts/render-profile.mjs && node --check scripts/validate-renderer.mjs",
    "validate:canvas": "node scripts/validate-renderer.mjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^0.1.100",
    "sharp": "^0.34.5"
  }
}
